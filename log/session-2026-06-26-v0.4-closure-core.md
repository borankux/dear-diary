# Session 2026-06-26 — v0.4 Closure Core

## Session Goal
Ship Dear Diary v0.4 Closure Core: keep the existing Vim-first Markdown journal stable while closing the AI processing loop:

```text
diary -> process -> candidates -> review -> todo/memory -> done/archive -> dashboard -> diary
```

## Key Actions (Chronological)
1. Recovered current project context from `PROJECT.md`, `session-handoff.md`, `TODO.md`, `UPDATE_LOG.md`, `docs/prd/main.md`, and the latest raw session log.
2. Added canonical diary-file filtering in `internal/storage` and reused it in search, process scanner, and dashboard loading.
3. Added additive SQLite migrations for `ai_candidates` plus source-evidence columns on existing `todos` and `memories`.
4. Changed `diary process` so LLM output writes pending candidates, not final todos/memories.
5. Added provider-neutral LLM config: `DIARY_LLM_*` preferred, `DEEPSEEK_*` compatible.
6. Added `diary review` for accept / reject / skip / quit.
7. Added `diary todo`, `diary todo done <id>`, and `diary todo archive <id>`.
8. Updated dashboard to show pending candidates, active todos, recent memories, today status, and filtered diary entries.
9. Persisted forced fatal process transitions into transition logs.
10. Updated README, PRD, PROJECT, TODO, UPDATE_LOG, DOCS, session handoff, and constitution candidates.

## Decisions & Rationale
| Decision | Rationale |
|---|---|
| Keep v0.4 to Todo / Memory only | Frank wants closure, not more asset types |
| Add `ai_candidates` instead of changing final tables directly | Prevent AI output from polluting trusted data |
| Preserve existing v0.3 todos/memories | Avoid data loss; migration is additive |
| Rejected candidates still participate in duplicate prevention | Prevent repeated resurfacing of content Frank already rejected |
| Use terminal review before rich TUI | Proves lifecycle first, UI polish later |
| Dashboard does not call AI | Dashboard exposes open loops; processing remains explicit |

## Output Files
- `cmd/diary/main.go`
- `internal/storage/storage.go` + tests
- `internal/search/search.go` + tests
- `internal/process/extractor.go`
- `internal/process/runner.go`
- `internal/process/store.go` + tests
- `internal/process/html.go` + tests
- `internal/process/scanner.go` + tests
- `README.md`
- `docs/prd/main.md`
- `PROJECT.md`
- `session-handoff.md`
- `TODO.md`
- `UPDATE_LOG.md`
- `DOCS.md`
- `.claude/candidates.md`

## Unfinished Items / Next Session Pickup
1. Run `diary process` on real new diary content and inspect candidate quality.
2. Use `diary review` to accept/reject a real batch.
3. Use `diary todo done/archive` to close accepted todos.
4. Decide from use whether `review edit`, `review merge`, or `diary memory` is actually needed.

## CLAUDE.md Candidates
- Closure over feature breadth: prioritize closing the existing Candidate -> Review -> Todo/Memory -> Done/Archive loop before adding questions, decisions, weekly review, local model gateway, or rich dashboard features.

## Verification
- `go test ./...` passed.
- `go vet ./...` passed.
- `make build` passed.
- `./bin/diary --version` printed `0.4.0`.
